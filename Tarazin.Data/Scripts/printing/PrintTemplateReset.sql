-- =============================================
-- Tarazin.Data/Scripts/printing/PrintTemplateReset.sql
-- «بازنشانی به قالب پیش‌فرض داخلی» — ردیف ذخیره‌شدهٔ قالب حذف می‌شود
-- (با آن DefaultFor هم از بین می‌رود) تا وضوحِ قالب به تعریف داخلیِ کد برگردد.
-- برخلاف Delete (که فقط IsSystem=0 را حذف می‌کند)، اینجا هر ردیف ذخیره‌شدهٔ
-- کاربر حذف می‌شود — قالب «سیستمی» واقعی در کد است، نه در این جدول.
-- =============================================
SET NOCOUNT ON;

DELETE FROM [printing].[PrintTemplates]
WHERE [Id] = @Id;