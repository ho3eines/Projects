@page "/settings"
<PageTitle>امکانات — حسابداری</PageTitle>
<h1 class="h-page-title">امکانات و جداول پایه</h1>
<p class="h-lead mb-4">شرکت‌ها، حساب کل / معین / تفصیلی.</p>
<div class="h-card h-card-pad text-muted">جداول پایه پس از تحقیق گزارشات طراحی می‌شوند. بعد از ساخت جدول، `_Seed.sql` را پر کنید.</div>
