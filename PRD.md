# PRD: ترازین — مدیریت هوشمند کسب‌وکار (Blazor Hybrid Platform)

## Overview
این سند معماری مصوب «ترازین» است: **۵ پروژه با لایه‌بندی یک‌طرفه** —
`Tarazin.Share` (مدل‌ها/قراردادها) ← `Tarazin.Data` (لایهٔ داده) ← `Tarazin.Ui`
(رابط کاربری مشترک، RCL) ← **دو هاست نازک** که همان UI را میزبانی می‌کنند:
1. **هاست وب** (`Tarazin.Web`) — Blazor Server در مرورگر.
2. **هاست MAUI** (`Tarazin.Maui`) — MAUI Blazor Hybrid (دسکتاپ ویندوز/مک و موبایل).

هیچ وب‌سرویس عمومی CRUD و هیچ کلاینت WASM جدا وجود ندارد. عملیات کسب‌وکار با
اسکریپت نامدار مستقیم انجام می‌شود؛ تنها API داخلی جدید broker محدود credential
برای MAUI است. رابط کاربری با **MudBlazor** ساخته می‌شود تا تیم درگیر طراحی دستی
نشود.

## Scope
- **محصولات (7)** — حسابداری، انبار آمل، خزانه‌داری، حقوق و دستمزد، طلافروشی،
  فروشگاه اینترنتی، پلتفرم مشترک (اخبار/بلاگ/گالری/کاربران/ممیزی).
- هر محصول = یک **ماژول** (`Tarazin.Ui/Modules/{Name}/`) با صفحات و یک **اسکیمهٔ
  SQL مستقل** (`Tarazin.Data/Scripts/{schema}/`).
- **مدل‌ها فقط در `Tarazin.Share`** (namespace: `Tarazin.Models`) — قرارداد دامنه.
- عملیات کسب‌وکار فقط از طریق **اسکریپت‌های TSQL نامدار** (Embedded Resource در
  `Tarazin.Data`) و Dapper اجرا می‌شوند. Web provider سمت سرور دارد؛ MAUI فقط
  فقط رشتهٔ اتصال را از broker امنیتی HTTPS به صورت **رمزگذاری‌شده** دریافت می‌کند و
  سپس همان عملیات مستقیم SQL را با DbService اجرا می‌کند. broker لایهٔ CRUD داده نیست.

## Architecture (High Level)
| لایه | مسئولیت | سازوکار |
|------|---------|---------|
| Share | مدل‌ها و قراردادهای مشترک (POCO) | `Tarazin.Share` (بدون وابستگی) |
| Data access | اجرای اسکریپت‌های نامدار روی SQL Server | `Tarazin.Data` — DbService + Dapper (در همان پروسه) |
| UI (مشترک) | همهٔ صفحات، فرم‌ها، جداول، مودال، اعتبارسنجی | `Tarazin.Ui` (RCL) + MudBlazor |
| Host وب | میزبانی UI در مرورگر | `Tarazin.Web` — Blazor Server (SignalR) |
| Host MAUI | میزبانی UI در اپ بومی | `Tarazin.Maui` — BlazorWebView (Blazor Hybrid) |
| Scripts | منطق دامنه و گزارش‌ها (report-first) | `Tarazin.Data/Scripts/{schema}/{Name}.sql` (Embedded) |
| DB | یک دیتابیس `TarazinMaster` با اسکیمهٔ جدا برای هر محصول | SQL Server (docker compose) |
| Auth | Web: PBKDF2 سمت سرور؛ MAUI: broker HTTPS با CustomerGuid + رشتهٔ اتصال فقط رمزگذاری‌شده از API (per-session AES) | `AuthService` + `CredentialBrokerService` + `/api/mobile/connection/encrypted` |
| Audit | ثبت تمام عملیات با زنجیرهٔ هش | `AuditService` → `[central].[AuditLog]` |

## Key Rules (قوانین کلیدی)
1. **وابستگی یک‌طرفه**: `Share ← Data ← Ui ← {Web, Maui}` — هرگز برعکس.
2. **UI فقط در `Tarazin.Ui`** — صفحات جدید آنجا ساخته می‌شوند تا هر دو هاست
   خودکار بگیرندش؛ هاست‌ها فقط پوسته‌اند.
3. **مدل‌ها فقط در `Tarazin.Share`**؛ **داده فقط در `Tarazin.Data`**.
4. **دو هاست، یک هسته** — `AddTarazinUiServices()` در `Program.cs` (وب) و
   `MauiProgram.cs` (MAUI)؛ `App.razor` مشترک در هر دو رندر می‌شود.
5. **MudBlazor تنها کتابخانهٔ UI است** — Bootstrap دستی، CSS سفارشی و جدول سفارشی ممنوع.
6. **هر ماژول فقط اسکیمهٔ خودش را لمس می‌کند** (`DbService` با `{schema}`).
7. **هیچ SQL خام در صفحات نیست** — همه چیز از `Data/Scripts/{schema}/{Name}.sql`
   (Embedded Resource در `ScriptCatalog`).
8. **Report-first**: قبل از ساخت هر ماژول، گزارش‌های موردنیاز دامنه تحقیق و سپس مدل‌ها
   و اسکریپت‌ها طراحی می‌شوند.
9. چهار بخش استاندارد هر ماژول: **ورود عملیات**، **عملیات ویژه**، **گزارشات**، **امکانات**.

## What was removed (delete-list)
- همهٔ پروژه‌های قدیمی: `webapi`، ۷ کلاینت WASM، `share`، `blazordeployservice`، `tests`.
- وب‌سرویس عمومی CRUD، token در URL و رمزنگاری اختصاصی AES با کلید client-side. تنها API جدید، broker محدود login/refresh/revoke روی HTTPS با token تصادفی و replay control است.
- بک‌بون رویدادها (Outbox) — cross-module عملیات مستقیماً و تراکنشی انجام می‌شود.
- Bootstrap سفارشی و کامپوننت‌های دست‌ساز (DataGrid و...) — همه با MudBlazor جایگزین شدند.

## MAUI Blazor Hybrid (خلاصه)
- `Tarazin.Maui/MainPage.xaml` → `BlazorWebView` با `RootComponent` = `Tarazin.App` (مشترک).
- `wwwroot/index.html` → رانتایم `_framework/blazor.webview.js` + استاتیک‌های MudBlazor و RCL.
- پیکربندی Embedded فقط `ServerEndpoint` عمومی HTTPS و `CustomerGuid` عمومی است؛ SQL connection/password، bootstrap secret، token و decryption key دائمی در MAUI ممنوع‌اند. شناسه از فرم ورود یا URL گرفته نمی‌شود.
- جریان (قانون): `Login(username, password) + CustomerGuid از appsettings.json → broker HTTPS → validation → POST /connection/encrypted → رمزگشایی per-session در حافظه → DbService مستقیم`. رشتهٔ اتصال فقط از API و فقط رمزگذاری‌شده می‌آید (`UseEncryptedMaster=false` رد می‌شود)؛ در logout نشست revoke و حافظه پاک می‌شود.
- رمزنگاری کانال SQL (`Encrypt=true`) الزامی است؛ اعتبارسنجی گواهی SQL طبق تصمیم مالک پروژه (۱۴۰۵/۰۵/۲۹) غیرفعال است (`TrustServerCertificate=true`). امکان استخراج secret فعال از حافظهٔ client compromise‌شده با رمزنگاری client-held حل نمی‌شود؛ عمر کوتاه، least privilege، RLS و revoke کنترل اصلی‌اند.
- build و رفتار SqlClient باید برای هر target MAUI در CI/E2E همان پلتفرم تأیید شود.

## Acceptance Criteria
1. `dotnet build Tarazin.Web/Tarazin.Web.csproj` — بدون خطا (شامل Share/Data/Ui).
2. `dotnet build Tarazin.Maui/Tarazin.Maui.csproj -f net8.0-windows10.0.19041.0`
   روی ویندوز با `dotnet workload install maui` — بدون خطا.
3. با `docker compose up -d` + `dotnet run` همهٔ ۹ ماژول از یک آدرس باز می‌شوند.
4. در صفحات هیچ SQL خام یا HTTP برای CRUD/business data وجود ندارد؛ broker credential و feed رسمی بازار تنها استثناهای محدودند.
5. اسکریپت‌های هر اسکیمه از اسکیمهٔ دیگر بدون اعلام قبلی استفاده نمی‌کنند
   (`tools/cross-schema-scan.sh` پاس شود).
6. ورود با کاربر bootstrap و password تزریق‌شده از secret store و مدیریت کاربران در هر دو هاست کار می‌کند؛ password پیش‌فرض وجود ندارد.
7. artifact نهایی MAUI فاقد connection string/password/token/key دائمی است و تست‌های fake GUID، inactive customer، replay، expiry، revoke، unauthorized و cross-customer پاس می‌شوند.

---
*نسخه ۲.۲ — ۲۰۲۶/۰۸/۱۲ — لایه‌بندی Share/Data/Ui + هاست وب (Blazor Server) + هاست MAUI.*

---

# بخش دوم — مدیریت تخصصی ارز و معاملات ارزی (PRD §34–§63)

> این بخش **جزو امکانات اصلی محصول** است، نه یک ماژول جانبی. نرم‌افزار علاوه بر
> مدیریت طلا و جواهر، یک **سیستم کامل مدیریت ارز و معاملات ارزی** دارد که بخشی
> از همان سیستم حسابداری است؛ تمام معاملات ارزی مستقیماً روی **حساب اشخاص،
> خزانه، موجودی ارز و حسابداری** اثر می‌گذارند.
>
> پیاده‌سازی: ماژول `currency` (اسکیمهٔ `currency` + مسیر `/currency`) — شرح
> فنی کامل در `docs/CURRENCY_MODULE.md`.

## 34. ارزهای پایه
حداقل ارزهای زیر پشتیبانی می‌شوند: ریال ایران، تومان، دلار آمریکا USD، یورو EUR،
درهم امارات AED، پوند انگلیس GBP، لیر ترکیه TRY، یوان چین CNY، دینار عراق IQD،
دینار کویت KWD، ریال عربستان SAR، فرانک سوئیس CHF، دلار کانادا CAD، دلار
استرالیا AUD، ین ژاپن JPY، روبل روسیه RUB — و امکان **تعریف ارز جدید توسط
مدیر** وجود دارد.

## 35. ریال و تومان
ریال و تومان دو واحد پولی مجزا ولی مرتبط (هر تومان = ۱۰ ریال). سیستم تبدیل
ریال↔تومان، نمایش مبالغ به هر دو واحد، ثبت مبلغ با واحد مشخص، تبدیل خودکار
هنگام ورود اطلاعات، کنترل اشتباه بین ریال و تومان، نمایش واحد پول در تمام
فرم‌ها و تعیین واحد پایه را پشتیبانی می‌کند. **واحد حسابداری پایهٔ سیستم ریال
است** و تمام ارزها و مبالغ در نهایت قابلیت تبدیل و گزارش‌گیری بر مبنای ریال
دارند.

## 36. کیف پول و موجودی ارز
برای هر ارز موجودی مستقل: مقدار موجودی، نرخ خرید، نرخ فروش، نرخ متوسط خرید،
ارزش ریالی، سود/زیان ارزی، گردش ارزی، موجودی اول دوره، ورود، خروج، انتقال،
موجودی پایان دوره.

## 37. خرید و فروش ارز در طلافروشی
خرید/فروش ارز هم‌زمان با طلا: ثبت مشتری/فروشنده، نوع ارز، مقدار، نرخ، مبلغ
کل، معادل ریالی، نحوهٔ تسویه (صندوق/بانک)، شماره معامله، تاریخ، ساعت و
توضیحات. سیستم خودکار: موجودی ارز را تغییر می‌دهد، موجودی ریالی را
به‌روزرسانی می‌کند، سود/زیان مورد انتظار را محاسبه می‌کند، طرف حساب را
به‌روزرسانی می‌کند، سند حسابداری می‌سازد و معامله را در گزارش معاملات ارز ثبت
می‌کند.

## 38. معاملات ترکیبی طلا + ارز + ریال
یک معامله می‌تواند چند نوع دارایی را هم‌زمان شامل شود (فروش طلا + دریافت دلار،
خرید آب‌شده + پرداخت یورو، فروش دلار + دریافت بخشی ریال و بخشی طلا، تهاتر طلا
با ارز، تهاتر ارز با کالا). سیستم همه را در یک عملیات ثبت و اثر مالی هر بخش را
جداگانه محاسبه می‌کند.

## 39. تبدیل ارز
بخش مستقل «تبدیل ارز»: هر ارز به هر ارز دیگر (دلار→ریال، ریال→دلار، یورو→ریال،
دلار→یورو، درهم→دلار و …). **واحد مرجع تمام محاسبات ریال است** — سیستم هر ارز
را ابتدا به ریال ارزش‌گذاری و سپس به ارز مقصد تبدیل می‌کند (`USD → IRR → EUR`).

## 40. موتور تبدیل ارز
برای هر تبدیل ثبت می‌شود: ارز مبدا، مبلغ مبدا، نرخ مبدا به ریال، ارز مقصد،
نرخ مقصد به ریال، مبلغ مقصد، کارمزد، نرخ نهایی، مبلغ نهایی، اختلاف نرخ،
سود/زیان تبدیل، تاریخ نرخ، منبع نرخ. نرخ به دو روش: **اتوماتیک** (نرخ سیستم) و
**دستی** (ورود کاربر).

## 41. کارمزد تبدیل ارز
کارمزد درصدی، مبلغ ثابت، به ارز مبدا، به ارز مقصد یا ریالی؛ سیستم مبلغ نهایی
را پس از کسر/اضافهٔ کارمزد محاسبه می‌کند.

## 42. نرخ خرید و فروش ارز
برای هر ارز: نرخ خرید، نرخ فروش، نرخ میانی، نرخ آنلاین، نرخ دستی، نرخ
حسابداری، نرخ معامله — و محاسبهٔ Spread.

## 43. مدیریت نرخ‌ها — مرکز قیمت‌ها
منوی «مرکز نرخ‌ها و قیمت‌ها» مرکز کنترل تمام قیمت‌های سیستم است: طلا (۱۸/۲۴/
۷۵۰/۲۰/۲۱/۲۲ عیار، مثقال، انس جهانی، دست دوم، آب‌شده)، سکه (امامی، بهار
آزادی، نیم، ربع، گرمی)، ارز (دلار، یورو، درهم، پوند، لیر، یوان، دینار، فرانک،
سایر) و فلزات (نقره، پلاتین، پالادیوم).

## 44. دریافت آنلاین نرخ‌ها
نرخ‌های بازار به‌صورت آنلاین از منابع تعیین‌شده دریافت می‌شوند: منبع اول
`tv.tablotala.app` و منبع دوم `matisagoldgallery.com/tablo` — هر دو به‌عنوان
**Price Source** قابل تعریف‌اند.

## 45. نمایش وضعیت منبع قیمت
برای هر نرخ: منبع (تابلو طلا / ماتیسا / دستی)، آخرین زمان دریافت، زمان آخرین
تغییر، نرخ قبلی، نرخ فعلی، درصد تغییر، مبلغ تغییر، وضعیت اتصال به منبع، وضعیت
معتبر بودن نرخ.

## 46. Override کردن نرخ آنلاین
نرخ آنلاین **هرگز مستقیم وارد معاملات نمی‌شود**: ۱) نمایش داده می‌شود ۲) مدیر
مشاهده می‌کند ۳) قابل ویرایش است ۴) نرخ اصلاح‌شده به‌عنوان «نرخ سیستم» ذخیره
می‌شود ۵) معاملات از نرخ سیستم استفاده می‌کنند ۶) نرخ آنلاین به‌عنوان مرجع
حفظ می‌شود.

## 47. انواع نرخ
Online Rate / Manual Rate / System Rate / Buy Rate / Sell Rate / Accounting
Rate / Transaction Rate — کاربر تعیین می‌کند هر عملیات از کدام نرخ استفاده
کند.

## 48. قفل قیمت هنگام معامله
نرخ استفاده‌شده در لحظهٔ معامله در خود معامله ذخیره می‌شود؛ تغییرات بعدی نرخ
بازار هیچ‌وقت معاملهٔ قبلی را تغییر نمی‌دهد.

## 49. تاریخچه نرخ‌ها
تمام تغییرات قیمت ذخیره می‌شود: مقدار قبلی، مقدار جدید، منبع، کاربر، تاریخ،
ساعت، نوع تغییر، آنلاین/دستی، دلیل تغییر — و امکان مشاهدهٔ نمودار تغییرات.

## 50. تبدیل همه ارزها به ریال
تمام گزارش‌های مالی قابلیت نمایش بر مبنای ریال دارند؛ ارزش کل دارایی با نرخ
جاری به ریال محاسبه می‌شود (ارزش دلار + یورو + درهم + طلا + سکه + موجودی
ریالی).

## 51. ارزش‌گذاری لحظه‌ای کل دارایی
صفحهٔ «ارزش لحظه‌ای دارایی»: موجودی ریالی، ارزها، طلای ۱۸/سایر عیارها،
آب‌شده، سکه، نقره، سایر دارایی‌ها — و در پایین ارزش کل به ریال + ارزش روز
گذشته، ارزش امروز، افزایش/کاهش و درصد تغییر.

## 52. سود و زیان ارزی
محاسبهٔ سود/زیان ناشی از تغییر نرخ (مثلاً خرید ۱۰,۰۰۰ دلار × ۱۸۰,۰۰۰ و بعد
نرخ ۱۸۷,۰۰۰) — **تفکیک از سود واقعی معامله**.

## 53. سود و زیان طلا + ارز
داشبورد مالی سود را تفکیک می‌کند: سود معاملات طلا، سود معاملات ارز، سود فروش
کالا، سود تغییر ارزش طلا، سود تغییر ارزش ارز، سود اجرت، سود سکه، سود آب‌شده،
سود خالص.

## 54. مرکز نرخ‌ها در معاملات
در فرم‌های خرید/فروش طلا و ارز، کاربر نرخ جاری را مستقیم می‌بیند (نرخ آنلاین |
نرخ سیستم) و با یک کلیک در معامله قرار می‌دهد؛ در صورت داشتن دسترسی، نرخ
معامله را تغییر می‌دهد.

## 55. کنترل دسترسی نرخ‌ها
سطوح دسترسی: مشاهده نرخ، دریافت نرخ، تغییر نرخ، Override نرخ، تغییر نرخ خرید،
تغییر نرخ فروش، تغییر نرخ طلا، تغییر نرخ ارز، مشاهده تاریخچه نرخ، تأیید نرخ.

## 56. بروزرسانی خودکار
فاصلهٔ بروزرسانی: ۱۰/۳۰ ثانیه، ۱/۵ دقیقه یا دستی (قابل تعیین توسط مدیر). نرخ
جدید ابتدا در «نرخ آنلاین» قرار می‌گیرد و تنها پس از سیاست تعیین‌شده توسط
مدیر وارد «نرخ سیستم» می‌شود.

## 57. مدیریت خطای منبع قیمت
در صورت در دسترس نبودن منبع: خطا ثبت می‌شود، آخرین نرخ معتبر حفظ می‌شود،
وضعیت منبع Offline می‌شود، کاربر مطلع می‌شود، سیستم از آخرین نرخ معتبر
استفاده می‌کند و ورود نرخ دستی فراهم است. **هرگز نرخ صفر یا نامعتبر وارد
معاملات نمی‌شود.**

## 58. اولویت منابع نرخ
اولویت منابع (TabloTala ← Matisa ← منبع سوم ← دستی)؛ در صورت عدم دسترسی منبع
اول، منبع بعدی بررسی می‌شود؛ مدیر ترتیب منابع را تغییر می‌دهد.

## 59. مقایسه منابع نرخ
مرکز نرخ‌ها نرخ منابع مختلف را کنار هم نمایش می‌دهد (TabloTala | Matisa | نرخ
سیستم) تا اختلاف منابع دیده و نرخ موردنظر انتخاب شود.

## 60. اصل قیمت‌گذاری واحد
تمام قیمت‌های سیستم (فروش طلا، خرید طلا، فروش ارز، خرید ارز، محاسبهٔ موجودی،
ارزش‌گذاری دارایی، محاسبهٔ سود، فروشگاه، گزارش‌ها) از **یک مرکز قیمت واحد**
تغذیه می‌شوند؛ در عین حال هر معامله نرخ نهایی خودش را ذخیره می‌کند.

## 61. دریافت اطلاعات سایت‌ها
دریافت نرخ از `tv.tablotala.app` و `matisagoldgallery.com/tablo` به API رسمی،
Feed یا روش مجاز و پایدار همان سرویس‌ها متکی است؛ اگر API عمومی در دسترس نبود،
سیستم به HTML Selector شکننده وابسته نمی‌شود (Endpoint و نگاشت‌ها در دیتابیس
قابل ویرایش‌اند) و قوانین/محدودیت‌های هر منبع رعایت می‌شود.

## 62. تجربهٔ نهایی کاربر طلافروشی
یک صفحه: تابلو قیمت (طلا: ۱۸/۲۴ عیار، مثقال، آب‌شده، انس؛ سکه: امامی، بهار
آزادی، نیم، ربع، گرمی؛ ارز: دلار، یورو، درهم، پوند، لیر، سایر) و کنار هرکدام
«نرخ آنلاین | نرخ سیستم | خرید | فروش | زمان بروزرسانی» و با یک کلیک اصلاح
نرخ سیستم.

## 63. هدف نهایی بخش طلا و ارز
تبدیل نرم‌افزار از حسابداری عمومی به **سیستم تخصصی مدیریت طلافروشی + معاملات
ارز**: طلافروش همهٔ دارایی‌های خود را در یک سیستم می‌بیند (ریال + تومان + دلار
+ یورو + درهم + سایر ارزها + طلای خام + طلای ساخته + آب‌شده + سکه + نقره +
جواهرات) و سیستم «مقدار هر دارایی → نرخ لحظه‌ای → ارزش ریالی → سود/زیان →
گردش → خرید → فروش → تسویه → حسابداری» را یکپارچه مدیریت می‌کند.

> **اصل نهایی:** هر دارایی با واحد اصلی خودش نگهداری شود، اما تمام دارایی‌ها در
> نهایت قابلیت ارزش‌گذاری، گزارش‌گیری و تجمیع بر مبنای ریال را داشته باشند.

---
*ضمیمهٔ بخش دوم — ۲۰۲۶/۰۸/۱۳ — مدیریت تخصصی ارز و معاملات ارزی (ماژول `currency`).*
