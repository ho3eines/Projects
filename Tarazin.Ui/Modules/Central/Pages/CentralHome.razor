@page "/central"
@inject UserSession Session

<PageTitle>پلتفرم مشترک — ترازین</PageTitle>

<PageHeader Title="پلتفرم مشترک" Subtitle="وبسایت شرکت، کاربران و ممیزی." Eyebrow="مرکز" />

<div class="tz-modgrid">
    @foreach (var card in Cards)
    {
        <ModuleCard Icon="@card.Icon" Title="@card.Title" Lead="@card.Lead"
                    Url="@card.Url" Accent="@card.Accent" />
    }
</div>

<MudText Typo="Typo.caption" Color="Color.Secondary" Class="mt-6">
    وضعیت نشست: @(Session.IsAuthenticated ? $"وارد شده — {Session.DisplayName} ({Session.RoleTitle})" : "خارج شده")
</MudText>

@code {
    record CardInfo(string Icon, string Title, string Lead, string Url, string Accent);

    static readonly List<CardInfo> Cards =
    [
        new(Icons.Material.Filled.Newspaper, "اخبار", "مدیریت اخبار شرکت", "/central/news", TarazinAccents.Steel),
        new(Icons.Material.Filled.EditNote, "وبلاگ", "پست‌های وبلاگ", "/central/blog", TarazinAccents.Ink),
        new(Icons.Material.Filled.PhotoLibrary, "گالری", "گالری تصاویر", "/central/gallery", TarazinAccents.Petrol),
        new(Icons.Material.Filled.Group, "کاربران", "مدیریت کاربران و نقش‌ها", "/central/users", TarazinAccents.Wine),
        new(Icons.Material.Filled.Policy, "ممیزی", "ممیزی رویدادهای سیستم", "/central/audit", TarazinAccents.Gold),
    ];
}
