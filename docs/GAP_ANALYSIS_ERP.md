# تحلیل شکاف — نرم‌افزار حسابداری جامع Enterprise (ترازین)

> تاریخ: ۲۰۲۶/۰۸/۱۳ · بر اساس بازبینی کد موجود `Tarazin` (Blazor Hybrid — ۵ پروژه)
> و مقایسه با لیست ۳۳‌بندیِ درخواست‌شده.
>
> **راهنمای وضعیت:**
> - ✅ موجود و کارا
> - 🟡 جزئی (بخشی از آن هست، بخش مهمی نیست)
> - ❌ وجود ندارد

## ۱. وضعیت فعلی پروژه (خلاصهٔ بازبینی)

معماری فعلی مطابق `skills/blazor/tarazin-project-architecture` و
`.agents/tarazin-tsql` است: پنج پروژه با وابستگی یک‌طرفه
(`Share ← Data ← Ui ← {Web, Maui}`)، دسترسی به داده با **Dapper + اسکریپت‌های
TSQL نامدارِ Embedded** (بدون HTTP برای دادهٔ کسب‌وکار؛ broker محدود MAUI فقط
credential موقت می‌دهد)، UI با **MudBlazor**، احراز هویت با **PBKDF2**، و ممیزی
tenant-aware است. یکپارچگی زنجیرهٔ هش ممیزی هنوز نیازمند اصلاح و تست پویا است.

ماژول‌های موجود (۸ اسکیمه): `central`، `accounting`، `inventory`، `treasury`،
`payroll`، `goldshop`، `store`، `currency` (ارز و معاملات ارزی — PRD §34–§63).
هر ماژول ۶ صفحه دارد (خانه/داشبورد/ورود عملیات/عملیات ویژه/گزارشات/امکانات)؛
ماژول ارز صفحات تخصصی `/currency/prices`، `/currency/wallets`،
`/currency/convert` و `/currency/combined` را هم دارد.

## ۲. نگاشت ۳۳ بخش درخواستی به وضعیت فعلی

| # | بخش | وضعیت | توضیح کوتاه |
|---|-----|:---:|-------------|
| 1 | حسابداری مالی | 🟡 | سند دوطرفه (`DocumentInsert` + کنترل توازن)، دفتر روزنامه/کل/تراز آزمایشی، بستن دوره. اما دفتر معین، تفصیلی شناور، انواع سند، گردش تأیید، ترازنامه/سود و زیان/جریان نقدی و مراکز هزینه نیست. |
| 2 | اشخاص و طرف حساب | 🟡 | `Parties` با نوع (مشتری/فروشنده/کارمند) + کد ملی. فاقد سقف اعتبار، مانده، چک‌ها، قراردادها و گروه‌بندی. |
| 3 | خزانه‌داری | 🟡 | صندوق/بانک/حساب بانکی/نرخ ارز، دریافت/پرداخت، جدول چک، بستن روز. فاقد انتقال بین حساب‌ها، تنخواه، چرخهٔ کامل چک و مغایرت بانکی. |
| 4 | خرید و فروش | ❌ | ماژول خرید (درخواست/سفارش/فاکتور خرید) وجود ندارد؛ فروش فقط از مسیر فروشگاه/طلافروشی. |
| 5 | انبار | 🟡 | چند انبار، رسید/حواله/تعدیل، کاردکس، موجودی، انبارگردانی، رزرو. فاقد سریال/بارکد/QR، سطوح قیمت و تاریخچهٔ قیمت. |
| 6 | طلافروشی تخصصی | 🟡 | اجناس طلا (با عیار)، قیمت روز، فاکتور فروش (وزن/اجرت/سود/مالیات). فاقد آب‌شده/انگ، سکه، تبدیل عیار، تهاتر و تسویهٔ طلایی. |
| 7 | قیمت‌گذاری لحظه‌ای | 🟡 | جدول `GoldPrices` + نرخ ارز + همگام‌سازی به اسنپ‌شات‌ها. فاقد نرخ خرید/فروش جدا، تاریخچهٔ نرخ و منبع خارجی. |
| 8 | POS / صندوق فروش | ❌ | وجود ندارد. |
| 9 | فروشگاه اینترنتی | 🟡 | محصول/مشتری/سبد/سفارش با چرخهٔ وضعیت و رزرو موجودی. فاقد پرداخت آنلاین، ارسال/رهگیری و تصاویر. |
| 10 | اموال و دارایی ثابت | ❌ | وجود ندارد. |
| 11 | کارگزینی/HR | 🟡 | فقط پروندهٔ پایهٔ کارمند. فاقد مرخصی/ماموریت/حضورغیاب/ارزیابی. |
| 12 | حقوق و دستمزد | 🟡 | کارمند، اقلام حقوق، فیش، نهایی‌کردن دوره + ثبت خودکار حسابداری/خزانه. فرمول‌ها ثابت‌اند (قابل تنظیم نیستند). |
| 13 | CRM | ❌ | فراتر از `Customers` پایه چیزی نیست. |
| 14 | مدیریت شعب | ❌ | موجودیت شرکت/شعبه وجود ندارد. |
| 15 | هزینه‌ها و درآمدها | ❌ | فقط از طریق حساب‌های معین؛ مرکز هزینه/پروژه ندارد. |
| 16 | اقساط و بدهی‌ها | ❌ | وجود ندارد. |
| 17 | مالیات | 🟡 | موتور `TaxRules` جدول‌محور (در طلافروشی اعمال می‌شود). فاقد صورتحساب الکترونیکی/وضعیت ارسال. |
| 18 | گزارش‌گیری حرفه‌ای | 🟡 | گزارش‌های پایهٔ هر ماژول. فاقد Excel/PDF/چاپ/نمودار/Drill-down/ذخیرهٔ فیلتر. |
| 19 | داشبورد مدیریتی | 🟡 | داشبورد هر ماژول (StatCard). داشبورد جامع مدیر و داشبورد طلا نیست. |
| 20 | هوش تجاری | ❌ | وجود ندارد. |
| 21 | جستجوی سراسری | ❌ | فقط جستجوی درون‌ماژولی. |
| 22 | گردش کار عملیات | 🟡 | وضعیت‌های ساده + رویدادهای Outbox بین‌ماژولی. Workflow رهگیری کامل نیست. |
| 23 | کنترل داخلی و حسابرسی | 🟡 | `AuditLog` tenant-aware، حذف منطقی، `IsDeleted` و ثبت کاربر/اسکریپت دارد؛ اما hash فعلی `PrevHash` را پوشش نمی‌دهد و درج سریال نیست. قفل اسناد نهایی و گزارش تغییرات «قبل/بعد» نیز نیست. |
| 24 | اعلان‌ها و هشدارها | ❌ | وجود ندارد. |
| 25 | چاپ و اسناد | ❌ | وجود ندارد. |
| 26 | تجربه کاربری | ✅ | MudBlazor + RTL + فونت فارسی + حالت روشن/تیره + واکنش‌گرا. |
| 27 | اتوماسیون | 🟡 | رویدادهای بین‌ماژولی (انبار→حسابداری/طلا/فروشگاه، حقوق→حسابداری/خزانه، قیمت طلا→اسنپ‌شات‌ها). |
| 28 | چندشرکتی/چندواحدی | ❌ | وجود ندارد. |
| 29 | تنظیمات منعطف | 🟡 | کدینگ خودکار، `TaxRules` جدول‌محور. بقیهٔ تنظیمات ثابت است. |
| 30 | تمرکز ویژه طلا | 🟡 | همان بند ۶/۷. |
| 31 | گزارش سود واقعی | ❌ | فقط ستون `Profit` در فاکتور؛ بدون بهای تمام‌شده/تغییر ارزش موجودی. |
| 32 | انواع کسب‌وکار | 🟡 | ماژول‌های عمومی چند کسب‌وکار را پوشش می‌دهند ولی پیکربندی نقش‌محور کسب‌وکار ندارد. |
| 33 | اصل یکپارچگی | ✅ | قراردادهای مشترک (`Tarazin.Share`) + اسکریپت‌های مصرف‌کننده با هدر `-- Cross-schema:` و گیت `tools/cross-schema-scan.sh`. |

## ۳. آنچه در این تغییر اضافه شد

مطابق درخواست، چهار کار زیر روی همین معماری (بدون افزودن پروژهٔ جدید) انجام شد:

### ۳.۱ کنترل دسترسی (RBAC) برای هر بخش
- **کاتالوگ دسترسی**: `Tarazin.Share/Permissions.cs` — ۴۶ دسترسی با الگوی
  `{module}.{action}` (view/entry/special/reports/settings/admin برای ۷ ماژول +
  ۴ دسترسی سیستمی `system.users/roles/audit/admin`).
- **نقش‌های پیش‌فرض**: مدیر سیستم، حسابدار، صندوق‌دار، انباردار، فروشنده،
  کارگزینی، حسابرس، فقط مشاهده.
- **دیتابیس**: جداول `[central].[Permissions]`، `[central].[Roles]`،
  `[central].[RolePermissions]` + ستون `Users.RoleId` (با FK و Migration
  idempotent) + اسکریپت‌های `PermissionSync/RoleSync/RolePermissionSync/
  UserRoleBackfill` که در استارت‌آپ از کاتالوگ C# همگام می‌شوند.
- **نشست**: `UserSession.HasPermission/CanView/HasAny` + بارگذاری دسترسی‌ها
  هنگام ورود.
- **اعمال در UI**: فیلتر منو و کارت‌های خانه بر اساس دسترسی، گارد مرکزی مسیر
  در `MainLayout` (نمایش «دسترسی ندارید»/«ابتدا وارد شوید»)، صفحهٔ جدید
  «نقش‌ها و دسترسی‌ها» (`/central/roles`) با ویرایشگر چک‌باکسی گروه‌بندی‌شده.

### ۳.۲ سرویس کاربر (UserService)
- `Tarazin.Ui/Services/UserService.cs`: ایجاد/ویرایش/حذف کاربر، دریافت کاربر،
  دریافت دسترسی‌های کاربر، مدیریت نقش‌ها و دسترسی‌ها (هش رمز PBKDF2 در خود سرویس).
- صفحهٔ `/central/users` و دیالوگ ویرایش کاربر (با انتخابگر نقش) از این سرویس
  استفاده می‌کنند.

### ۳.۳ CreatedAt / UpdatedAt برای همهٔ بخش‌ها
- **همهٔ جداول موجودیت** (`_Ensure.sql` هر اسکیمه) ستون‌های
  `CreatedAt/UpdatedAt/CreatedBy/UpdatedBy` را (با Migration idempotent) دارند.
- **اسکریپت‌های Upsert** هنگام ویرایش `UpdatedAt` (و در جداول مرکزی `UpdatedBy`)
  را ثبت می‌کنند؛ **اسکریپت‌های List/Search** آن‌ها را برمی‌گردانند؛
  **مدل‌های `Tarazin.Share`** همه `CreatedAt/UpdatedAt` دارند.
- استثناهای عمدی: جداول زیرساختی/پیوست‌نما (`Outbox`، `Sessions`، `AuditLog`،
  `*Snapshot`، `CartItems`) که پیوست‌فقط هستند یا معنای ممیزی خودشان را دارند.

## ۴. چرا بخش‌های «ندار» را یک‌جا نساختم

لیست ۳۳‌بندی عملاً یک ERP کامل است. در این تغییر، تمرکز روی چهار خواستهٔ صریح
شما (دسترسی برای هر بخش + سرویس کاربر + CreatedAt/UpdatedAt) و زیرساختِ لازمِ
آن‌ها بود. مابقی بخش‌ها (اموال، اقساط، POS، BI، اعلان‌ها، …) به‌عنوان Backlog
در `todo.md` ثبت می‌شوند و هرکدام با همان الگو (گزارش‌محور → مدل → اسکریپت →
صفحه) قابل افزودن‌اند.

## ۵. بخش دوم PRD — مدیریت تخصصی ارز و معاملات ارزی (بندهای ۳۴ تا ۶۳)

مطابق «دستخط» مصوب (PRD §34–§63)، ماژول درجه‌یک `currency` روی همین معماری
ساخته شد — **جزو امکانات اصلی محصول**، نه ماژول جانبی:

| بند | عنوان | وضعیت | محل پیاده‌سازی |
|-----|-------|:---:|----------------|
| 34 | ارزهای پایه (۱۶ ارز + سفارشی) | ✅ | `currency.Currencies` + `CurrencyUpsert` + صفحهٔ امکانات |
| 35 | ریال و تومان (واحد پایه = ریال) | ✅ | `Currencies.UnitFactor` (TOMAN=10) + `Settings` + مبدل نمایشی در امکانات |
| 36 | کیف پول و موجودی ارز | ✅ | `Wallets` + `WalletList` (موجودی/متوسط خرید/ارزش/سود/گردش/اول و آخر دوره) |
| 37 | خرید/فروش ارز در طلافروشی | ✅ | `FxTransactionCreate` — کیف پول + خزانه + سند حسابداری + طرف حساب + تاریخچه در یک تراکنش |
| 38 | معاملات ترکیبی طلا+ارز+ریال | ✅ | `FxCombinedCreate` + `FxTransactionLegs` (اثر مالی هر پای جدا) |
| 39–41 | تبدیل ارز + موتور تبدیل + کارمزد | ✅ | `ConvertPreview`/`ConvertExecute` (مرجع ریال؛ کارمزد درصدی/ثابت/مبدا/مقصد/ریالی) |
| 42 | انواع نرخ (خرید/فروش/میانی/آنلاین/دستی/حسابداری/معامله + Spread) | ✅ | `PriceRates` (ستون‌های مجزا + MidRate/Spread) |
| 43 | مرکز نرخ‌ها و قیمت‌ها (طلا/سکه/ارز/فلز) | ✅ | `PriceItems` + `RateBoard` + صفحهٔ `/currency/prices` |
| 44–45 | دریافت آنلاین + وضعیت منبع | ✅ | `PriceSources` + `PriceFeedService` + نمایش وضعیت/زمان/تغییر/اعتبار |
| 46 | Override نرخ آنلاین | ✅ | `RateOverride` + IsOverride (نرخ آنلاین هرگز مستقیم وارد معامله نمی‌شود) |
| 47 | انواع نرخ (۷ نوع) | ✅ | `PriceRates` + `RateUpsert` (RateKind) |
| 48 | قفل قیمت هنگام معامله | ✅ | `Rate` در `CurrencyMovements`/`FxTransactionLegs` + `RateHistory(Transaction)` |
| 49 | تاریخچهٔ نرخ + نمودار | ✅ | `RateHistory` + `RateHistoryChart` + نمودار MudChart در گزارشات |
| 50–51 | ارزش کل به ریال + ارزش لحظه‌ای دارایی | ✅ | `AssetValuation` + `AssetValuationSnapshot` + صفحهٔ `/currency/assets` |
| 52–53 | سود/زیان ارزی و تفکیک طلا+ارز | ✅ | `RealizedPnl` در پاها + `PnlSummary` (محقق/محقق‌نشده/اجرت/سکه/آب‌شده/خالص) |
| 54 | مرکز نرخ در فرم معاملات | ✅ | چیپ «نرخ آنلاین/سیستم» + کلیک برای قرار دادن نرخ در معامله — ارز (`/currency/entry`) و طلا (`/goldshop/entry` ← `goldshop/GoldRateBoard`) |
| 55 | کنترل دسترسی نرخ‌ها | ✅ | `Permissions.cs` → `rates.*` (۱۰ دسترسی) + گارد UI |
| 56 | بروزرسانی خودکار | ✅ | `PriceFeedScheduler` + تنظیم فاصله + سیاست AutoPromote |
| 57 | مدیریت خطای منبع | ✅ | `PriceSourceStatus` (Offline/خطا/ErrorCount) + حفظ آخرین نرخ معتبر |
| 58 | اولویت منابع | ✅ | `PriceSources.Priority` + چرخهٔ Fallback در `FetchAllAsync` |
| 59 | مقایسهٔ منابع | ✅ | `PriceSourceValues` + `RateComparison` (TabloTala\|Matisa\|سیستم) |
| 60 | مرکز قیمت واحد | ✅ | همهٔ مصرف‌کننده‌ها از `PriceRates`؛ معامله نرخ خودش را قفل می‌کند؛ فرم فروش طلا هم از مرکز قیمت تغذیه می‌شود |
| 61 | دریافت با API/Feed رسمی (بدون HTML Selector) | ✅ | Endpoint + MappingsJson در دیتابیس؛ JSON/JS ساختارمند فقط |
| 62 | تجربهٔ تابلو قیمت یک‌صفحه‌ای | ✅ | صفحهٔ `/currency` (تابلو + اصلاح یک‌کلیکی) |
| 63 | هدف نهایی (یک سیستم دارایی یکپارچه) | ✅ | `AssetValuation` + کیف پول + مرکز نرخ + سود/زیان یکپارچه |

**نکته:** `treasury.CurrencyRates` (نسخهٔ قدیمی/ساده) برای سازگاری باقی مانده
است؛ دادهٔ حرفه‌ای نرخ اکنون در `currency.PriceRates` است و همهٔ بخش‌های جدید
از همان تغذیه می‌شوند.


**اصلاحات و تکمیل‌های بازبینی (همین نشست):**
- §۳۵: گارد «ریال/تومان در کیف پول معاملاتی ممنوع» در `FxTransactionCreate` + فیلتر از لیست خرید/فروش و معاملهٔ ترکیبی + نمایش «واحد» زیر فیلد مقدار.
- §۳۶: گردش دستی ورود/خروج/انتقال/تعدیل (`WalletMovementManual` + دیالوگ در `/currency/wallets`).
- §۴۲/§۴۷: نمایش ستون «نرخ حسابداری» در مرکز نرخ‌ها.
- §۴۱: سند حسابداری کارمزد برای هر سه نوع (مبدا/مقصد/ریالی) متوازن و هماهنگ با خزانه (دریافت کارمزد ریالی در صندوق/بانک).
- §۵۴: فرم فاکتور طلا (`/goldshop/entry`) نرخ آنلاین/سیستم را از مرکز قیمت نشان می‌دهد و با یک کلیک در فاکتور قرار می‌دهد.
